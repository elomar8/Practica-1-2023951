import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Practica-1-2023951',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
